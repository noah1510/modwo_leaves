#include <iostream>
#include <fstream>
#include <vector>
#include <string>

using namespace std;

const int BILDGROESSE = 256;

int* convimg (string path);

//vector < vector <int> > convertImage(string bob);